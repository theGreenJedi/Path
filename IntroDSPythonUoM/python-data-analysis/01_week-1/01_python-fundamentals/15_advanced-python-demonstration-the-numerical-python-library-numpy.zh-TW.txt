大家好， 今天，我們將學習Numpy，這是一個廣泛應用於數據科學界的軟體套件， 它使我們能夠高效地工作在Python中的陣列(arrays)和矩陣(matrices)。 首先，我們將Numpy導入為np。 這讓我們使用快捷方式np來引用Numpy。 我們可以先建立陣列 我們可以先建立一個列表, 並
將其轉換為陣列。 所以這是我們的第一個Numpy陣列。 我們可以通過直接傳遞列表來更簡潔地做到這一點。 現在讓我們通過傳遞列表列的表來創建多維陣列。 我們通過了兩個列表，每個都有三個元素，我們得到一個2x3的陣列。 我們可以使用shape屬性來檢查尺寸。 對於arange函數，我們傳遞一個開始start，一個停止stop和一個跨步step的值， 並在給定的間隔內返回均勻跨步的值。 所以我們想把這個陣列轉換成一個3x5的陣列。 我們可以使用reshape來做到這一點。 linspace函數類似於arange，除了我們告訴 我們需要返回多少個數字，它會相應地分隔間隔。 我們可以使用resize來到位改變維度數(dimention)。 Numpy還有幾個內置函數和快捷方式來創建陣列。 ones返回一個都是1的陣列，
zeros是一個都是0的陣列。 eye返回一個陣列，其中對角線是1，其他是0的，和 diag構造一個對角陣列。 要重複創建一個陣列，我們可以傳入一個重複的列表，或者 我們可以使用Numpy的repeat函數。 注意兩個輸出之間的差異。 我們也可以組合陣列來創建新的陣列。 讓我們創建一個都是1的2x3陣列， 並將其自身垂直堆疊，乘以2. 這裡是一樣的，但是水平堆疊。 現在，我們來看看你可以用Numpy陣列做的一些運算 執行逐元素的加法，減法，乘法和 除法是直截了當的，相同的將陣列的所有數字提高一個乘冪(power)。 對於那些熟悉線性代數， 可以使用dot函數來完成dot product。 我們來創建一個使用前一個陣列y及其平方值的新陣列。 這個陣列的shape是2x3。 我們也可以使用T方法來轉置陣列， 該方法交換行和列。 轉置的陣列的形狀(shape)是3x2。 使用dtype，我們可以看到陣列具有什麼類型的數據， 使用astype，將陣列轉換為不同的類型。 Numpy還有許多我們可以使用的有用的數學功能 我們來看幾個常用的。 這是我們的新陣列a 我們可以查看陣列中的值的總和(sum)，最大值(max)和 最小值(min)，或 平均值(mean)和標準偏差(std)。 要找到最大或最小值的索引，我們可以使用argmax和argmin。 接下來，我們來了解索引(index)和切片(slice)。 讓我們創建一個陣列，0到12的平方. 我們可以使用方括號表示法獲取特定索引的值， 並用冒號(:)符號獲取範圍。 冒號(:)符號是開始，結束，和跨步的數值。 指定起始或結束索引不是必需的。 而且我們也可以使用負數從陣列的末尾倒數。 對於我們的第一個例子，我們來看看從索引1開始的範圍， 並在索引5之前停止。 接下來，我們來看一下陣列的最後四個元素 而在這裡，我們從陣列的末尾開始第五， 每步向後倒數二。 我們來看看這是如何擴展到二維陣列的。 首先，我們做一個二維陣列，0到35. 我們可以使用逗號(,)表示法獲得一個特定的值。 這是第二行(row)和第二列(column)的值。 現在我們用冒號表示法來獲取第三行和第 三到第六的列。 我們也可以做一些事情，比如獲取前兩行和 除最後一列之外的所有列。 這是我們如何從最後一行中選擇每一個第二個元素。 我們也可以使用方括號運算符進行條件索引和賦與值。 例如，這將返回一個陣列，它是我們 原始陣列的大於30的元素。 這個賦值將把原來陣列中的元素 分配給一個新的值。 在這裡，我們將陣列中的元素的最大值上限到30 接下來，我們來看看在Numpy中複製數據。 首先，我們創建一個新的陣列r2，它是陣列r的一個切片。 現在，我們把這個陣列的所有元素設置為零。 當我們看原始陣列r時， 我們可以看到r中的slice也被改變了。 所以這是需要記住的， 在使用Numpy陣列時要小心。 如果我們希望創建副本，但不會更改原始陣列r， 那麼我們可以使用r.copy()。 我們可以看到，如果我們將r_copy中所有元素的值更改為10， 則原始陣列r保持不變。 最後，我們來學習如何迭代陣列。 首先，我們創建一個4x3陣列，用從零到九的隨機(random)數字，。 例如，我們可以通過在行(row)中輸入行進行迭代。 我們可以迭代行索引，通過使用test的len函數進行， 它(len)返回行數。 我們可以通過使用enumerate結合這兩種迭代方式， 這給我們行和行的索引。 讓我們做一個新的陣列，test2。 如果我們希望遍歷這兩個陣列，我們可以使用zip。 Numpy有很多可以提供。 所以一定要查看文檔來了解更多優秀的功能。 謝謝加入我們。 希望下次見到你。