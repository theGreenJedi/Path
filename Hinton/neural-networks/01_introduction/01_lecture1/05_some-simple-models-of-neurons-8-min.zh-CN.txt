在这个视频中 我将介绍一些相对简单的神经元模型 我将介绍几种不同的模型
首先是简单的线性阈值神经元 然后还会介绍一些稍显复杂的模型 虽然这些模型比真正的神经元简单得多
但它们仍然具有足够的复杂性 足以允许我们用来构成神经网络
开展一些有趣的机器学习 为了理解一个复杂的东西 我们必须对其进行理想化
即做一些简化 使得我们能够初步理解它是如何工作的 打个比方 我们把原子内部运行简化成微小的太阳系 理想化的过程去除了那些对于理解主要规律
并不关键的复杂细节 这使得我们能够对其进行数学处理 并与其他相似系统进行类比
一旦我们理解了基本原理 就可以增加模型的复杂性
让它能更好地还原现实 当然 在理想化的过程中 我们要特别小心 不能简化掉那关系到主要性质的东西 去了解那些已经被认为是错误的模型 常常是值得的 只要我们别忘了它是错的 比如说 很多神经网络的研究使用的神经元
相互传递的是实值 而不是离散的活动峰值
我们知道真正的脑细胞并不是这样工作的 但这些系统依然值得我们去理解 在机器学习中他们其实是非常有用的 我要介绍的第一种神经元是最简单的线性神经元 它很简单 它所能做的在计算上有所限制 它能让我们深入探究更复杂的神经元 但它也可能会有一点误导倾向
回到正题 在一个线性神经元中 输出Y 是这个神经元的偏置 (bias) b 以及 所有的输入连接与其对应的突触权值的乘积
的函数 如果将它画出来 X轴是偏置加上加权和 将会得到一条过原点的直线 和线性神经有很大不同的
是阈值型神经元 这个模型由麦卡洛克和皮兹发明
他们实际上在冯诺依曼思考如何设计通用计算机时 对其造成了不少影响 在阈值型神经元中
你首先计算输入信号的加权和 如果加权和超过了阈值
则发出一个峰值信号 麦卡洛克和皮兹认为
这些峰值就像是一系列命题的真值 每个神经元 通过组合它从其他神经元得到的真值
来产生它自己的真值 这就像组合一些命题
来计算另外一个命题的真值 在1940年代 逻辑被认为是大脑工作的主要模式 从那时起 那些在思考大脑如何工作的人 将兴趣更多地转移到了 大脑是如何组合大量不同来源的不可靠的数据 所以逻辑并不是决定大脑如何运行的一个好的模式 对于一个阈值型神经元来说
你可以把它的输入输出函数 看做决定于加权和是否高于阈值
如果是的话 则输出为1 否则 输出为0
实际上有两种等价的方式 来给出阈值型神经元的激活方程 我们可以说 
总的输入z 就是各输入信号乘上对应的权重 之和 如果z 大于阈值 则输出Y为1 否则为0
或者我们也可以说 输入还包含了一个偏置量
所以总的输入就等于 各输入信号乘上对应权重 之和
再加上一个偏置量 然后如果 总输入大于0 则输出为1 否则 输出为0
等价性是显而易见的 第一个方程中的阈值
就等于第二个方程中的偏置量的负值 一种结合了 线性和阈值型神经元性质的神经元
就是分段线性神经元 它首先计算所有输入的权重和 但是它的输出并不是这个权重和的线性函数 即我们按照之前的方法计算权重和 z 如果 z 小于0 则输出为0 否则 输出为z 可以看到 z大于0的部分是线性的
而等于0时 则是不连续的 所以输入输出的曲线看起来是这样的
它显然不是线性的 但对大于0的部分 它却是线性的
使用这样一个神经元 我们能在大于0的部分
得到线性系统的很多优良性质 我们也可以在等于0的地方
做出决策 这门课程中我们使得很多的一种神经元
可能也是在人工神经网络中 使用得最广泛的神经元
是S型神经元 这种神经元的输出它的总输入的
一个平滑而有界的函数 通常使用的是逻辑函数 (logistic function) 总输入z 的计算方式和之前相同
即偏置量 加上权重和
其输出则是 1 加上 e的负z次方分之一 想一想 如果权重和是个很大的正数 取反之后放到e的指数位
结果将接近0 所以整个输出将会是1
如果权重和是个绝对值很大的负数 那么指数项将会是个很大的正数
整个输出将会是0 所以输入输出函数看起来就像这样 当总输入时0时 e 的0次方是1 所以输出就是0.5
S型曲线的一个优良性质是 它有光滑的导函数
即它的导函数是连续的 这些优良性质 
让我们更容易使用它来进行学习 正如我们将在第三讲里看到的那样
最后是随机性阈值型神经元 这种神经元使用与逻辑型神经元一样的方程 它首先按同样的方法计算出总输入
然后用逻辑函数计算出一个实数 这个实数将作为输出信号的概率 它并不是直接输出这个概率 而是基于这个概率做一个选择
它的输出实际上是要么1 要么0 从本质上来说 这个输出的随机的 所以这种神经元将 p 作为输出为1 的概率
而不是一个实际的输出 当然 如果总输入是非常大的正数 输出将几乎总是1 如果输入是一个绝对值很大的负数
那么输出将几乎总是0 我们可以对分段线性神经元做类似的处理 我们可以说 分段函数的输出值
那个实数值 如果大于0的话 就是产生信号的速率 这是决定性的
但是如果我们把信号产生的速率 即信号实际上产生了多少次
看成一个随机过程 它其实就是泊松分布 所以分段线性函数决定了信号产生的速率
而它内在的随机性 则决定了这些信号什么时候产生 
 翻译 Pan Zhimeng