Como veremos en las próximas clases, puede
ser muy útil tener secuencias de números que representan los índices de
cadenas o listas. En esta clase, voy a introducir la
función "range" de Phyton y vamos a usarla para este propósito.
En este primer ejemplo, usaremos un bucle "for" -for loop- en conjunción con "range" -intervalo-, para
generar y visualizar una secuencia de enteros desde cero hasta, e incluyendo
nueve. Entonces, para cada número n, y aquí es donde
usamos "range", range va a generar esa secuencia de números. Voy a pasar el
valor diez, este es el valor de, de del máximo de esta secuencia.
Voy a imprimir cada número de la secuencia. Los números que vemos en la pantalla son
valores enteros de cero a nueve. El valor inicial por defecto es
cero. El valor final es 10. 
Entonces nuestro último número es 9. Y el paso -step-, o la diferenceia entre
dos valores cualesquiera de la secuencia, es por defecto, uno.
Miremos la ayuda para range. La ayuda es bastante larga, pero quiero
subrayar el encabezamiento. Podemos ver por este encabezamiento que hay 
hasta tres argumentos que podemos pasar a la función.
El que dimos en nuestro ejemplo fue el "stop", el valor en el cual detenerse.
Opcionalmente podemor proveer un valor de inicio para el cual el defecto es cero, y tambien
proveer un step, que si lo omitimos entonces se utiliza el defecto que, como ya mencionamos y mostramos
en el ejemplo, es un paso -step- de uno. Una de las formas en que usamos range es para
generar una secuencia de números que representen los índices de una cadena o
una lista. Entonces para la cadena "computer science", usaremos
range para generar esos índices. "Computer science" tiene dieciséis caracteres.
Entonces los valores de los índices son: cero, uno, dos, y así hasta quince.
Podemos generar esos valores usando range. Puedo pasar dieciséis como argumento 
de range. Pero para hacerlo más general puedo decir,
aumenta los números hasta llegar al largo de la cadena. Noten que estoy usando i para representar el
índice de S, entonces i es índice abreviado. Y se imprimen los valores desde cero
a quince. Pero quizá, sólo queremos los índices
empezando por el uno. Podemos usar el parámetro start opcional
para indicar eso. Entonces puedo pasar umo para el índice de comienzo,
y después seguir con toda la longitud de la cadena.
Y cuando termine, imprimir los valores, esta vez vemos que empezó por el uno, subiendo de a uno y
terminando e incluyendo quince. Ahora, digamos que sólo queremos los
índices impares. Para hacer esto, podemos usar los tres
parámetros. Entonces el primero es el parámetro start,
empezar en uno. El end, o valor de detenerse.
Y ahora el step. Por defecto, el step es uno, así que vamos por
los valores uno a dos a tres. Voy a aumentar el step a dos,
y eso me va a dar los valores uno, tres, cinco, siete y así sucesivamente.
Y eso nos da los índices impares de la cadena S.