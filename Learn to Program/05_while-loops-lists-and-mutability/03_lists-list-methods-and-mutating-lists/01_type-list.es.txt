.
En nuestros programas a veces trabajamos con colecciones de datos.
Por ejemplo, podemos trabajar con las notas estudiante o con los títulos de los libros.
En esta clase les introduciré a el tipo de lista de Python y exploraremos 
algunas de las funciones y operaciones que se puede aplicar a las listas. 
Para empezar voy a crear una variable grades que se refiere a una lista de tres elementos 
de enteros. La notación para nuestra lista es un corchete de apertura 
Valores separados por comas y un corchete de cierre.
Grades es una lista de tres elementos. donde decimos que grades tiene tres elementos
El primer elemento está en el índice cero y grades en el índice cero se refiere a la enésima 80. 
Grades en el primer índice se refiere al enésimo 90 y grades en el índice dos se refiere a la enésima 70. 
También podemos tomar las rebanadas de una lista como podemos con una cadena que comienza con un
índice inicial. En este caso, voy a empezar en el índice inicial
Y un índice final. Así, subiendo a dos, esto me da una rebanada 
de la lista que contiene un único int 90.
Si voy en grades desde cero hasta dos luego se da una rebanada de dos elementos con 80 y
90. Así las mismas reglas aplican para rebanar a 
listas como para las cadenas. El operador n que hemos usado para las cadenas 
también se puede usar con listas. Así que esto pregunta si 80 es un elemento de
la lista grades, que lo es. La expresión 60 en grades evalúa a
falso desde 60 no es un elemento de la Lista de los grades. 
Algunas de las funciones incorporadas de Python que hemos utilizado anteriormente con otros tipos también pueden 
se aplicadas al tipo lista. Por ejemplo, podemos encontrar la longitud de una
lista, en este caso tres, porque la lista tiene tres elementos
También podemos encontrar el valor más pequeño en una lista pasando la lista como argumento para 
la función MIN, y cuando llamamos a la función MAX en una lista, devuelve el
elemento más grande. Hay otra función llamada SUM. 
Va a sumar los elementos de la lista. Así en este caso suma 80, 90, y 70 
retornando 240. Grades es una lista de INTs, pero podemos tener
listas de otros tipos, también. Esta variable va referirse a una
lista de cadenas. Algunas de las mismas funciones que aplicamos 
a la lista de INT también se puede aplicar a la lista de cadenas 
Por ejemplo, podemos encontrar su longitud, y podemos averiguar el valor mínimo en
la lista. Para cadenas, el orden del diccionario es a lo que
se compara, y el El mínimo en este caso es el archivo. 
El max. Va a ser una cadena que viene 
finalmente en el alfabeto, o el por último en el diccionario, que es math. 
Y la función sum no se puede aplicar a cadenas.
Un error de tipo se produce cuando sum se aplica a la la lista de cadenas.
Los datos de diferentes tipos pueden estar contenidos en la misma lista. 
Por ejemplo, vamos a hacer una lista de dirección de calle
Contará con un int para el número de la calle, y una cadena para el nombre de la calle
Así que tienes un entero y una cadena en la misma lista. 
La última cosa para muestrarles en este Introducción a las listas es la iteracción for
sobre las listas. Por lo tanto, echemos un vistazo a nuestro ejemplo grade.
Para cada nota en la lista grades, haremos una iteración sobre ella y lo mostraremos
Grade es un nombre de variable. Podría haber usado aquí algo así como un item o 
element. Así que vamos a imprimir estas tres grades.
Del mismo modo podemos iterar sobre los subjects. Y yo voy a cambiar el nombre de la variable sólo para
hacen hincapié en que, eso es sólo una variable, refiriendo a cada elemento de la lista en 
turno, a partir del índice cero, trabajando hacia el final de la lista,
Imprimiendo los subjects