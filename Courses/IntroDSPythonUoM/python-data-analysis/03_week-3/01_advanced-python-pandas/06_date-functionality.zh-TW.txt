歡迎來到今天的教程， 我們將在Pandas中查看時間序列和日期功能。 Pandas有四個主要的時間相關類型。 Timestamp，DatetimeIndex，Period和PeriodIndex。 首先，我們來看一下Timestamp。 Timestamp表示單個時間戳，並將值與時間點相關聯。 例如，讓我們創建時間戳使用字串 '9/1/2016 10:05AM'，這裡我們有我們的時間戳。 在大多數情況下，Timestamp可以與Python的datetime互換。 假設我們對特定時間點不感興趣， 而是想要一段時間。 這就是使用Period的時候。 Period代表單個時間跨度，例如特定的日期或月份。 這裡我們創建一個2016年1月的Period， 這裡是2016年3月5日的一個Period例子。 timestamp的索引(index)是DatetimeIndex。 我們來看一個簡單的例子。 首先，我們創建我們的示例系列t1， 我們將使用2016年9月1日，2月和3日的Timestamp。 當我們看這個系列時，每個時間戳是索引 並且具有與之相關聯的值，在這種情況下，a，b和c。 看看我們的系列索引的類型，我們看到它是DatetimeIndex。 同樣，期間(Period)索引是PeriodIndex。 讓我們再創一個示例系列t2。 這一次，我們將使用值d，e和f， 並將其與2016年9月，10月和11月的時期匹配。 看看ts2.index的類型，我們可以看到它是PeriodIndex。 現在，我們來研究如何轉換為日期時間。 假設我們有一個列表，字串為日期。 如果我們使用這些日期作為索引創建一個DataFrame。 和一些最低限度生成的數據，這是我們得到的DataFrame。 看一下這個索引，我們可以看到它很混亂， 日期都是不同的格式。 使用Pandas的to_datetime，Pandas將嘗試將它們轉換為 日期時間，並將其放在標準格式中。 to_datetime還有更改日期解析順序的選項。 例如，我們可以通過參數dayfirst=True來 解析歐洲日期格式的日期。 時間差異是時間的差異。 我們可以看到，當我們取之間差異，在9月3日和 9月1日時， 我們得到了兩天的Timedelta。 我們也可以做一些事情找到日期和時間，比如12天 和3個小時之後，從在9月2日上午8:10開始計數。 接下來，我們來看看在DataFrame中處理日期的一些技巧。 假設我們要查看九個測量，每週兩次， 每個星期日， 從2016年10月開始。 使用date_range，我們可以創建這個DatetimeIndex。 現在，讓我們使用這些日期和一些隨機數據創建一個DataFrame， 看看我們可以做些什麼。 首先，我們可以檢查一個特定日期的星期幾。 例如，我們可以看到，索引中的所有日期都在星期日。 我們可以使用diff來查找每個日期值之間的差異。 假設我們想知道DataFrame中每個月的平均數是多少。 我們可以使用resample來做到這一點。 我們可以使用部分字串索引來查找特定年份或 特定月份的值，或者我們甚至可以在一系列日期中進行切片。 例如，這裡我們只想要從2016年12月算起的值。 我們可以做的另一件很酷的事情是在DataFrame中更改我們日期的頻率， 使用asfreq。 如果我們使用這種方式將頻率從每兩週更改為每週， 我們每隔一周就會丟失值。 所以讓我們對這些缺失的值使用正向填充ffill方法。 最後一件我想簡要介紹的是繪製時間序列。 導入matplotlib.pyplot，並使用iPython 魔法 ％matplotlib inline， 將允許您可視化筆記本中的時間序列。 在下一課程中，我們將進一步了解理解和 創建可視化。 感謝觀看，下次見